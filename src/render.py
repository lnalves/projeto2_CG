"""
Saída: imagem anotada + tabela.
Desenha caminhos dos 2 segmentos, marca os 3 pontos, rotula id + valor.
Exporta tabela (pandas → CSV/Excel).
"""

from __future__ import annotations


def annotate_image(image, results, config):
    """Retorna imagem anotada."""


def export_table(results, out_path):
    """Exporta CSV com id, segmento1_mm, segmento2_mm, total_mm."""
